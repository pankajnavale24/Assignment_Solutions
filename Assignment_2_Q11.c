//Write a program to input a number from the user and also input a digit. Append adigit in the number and print the resulting number.
// (Example - number=234 and digit=9 then the resulting number is 2349)
#include<Stdio.h>
int main()
{
    int x,y;
    printf("enter a number");
    scanf("%d",&x);
    printf("enter a digit");
    scanf("%d",&y);
    printf(" resulting number %d%d",x,y);
}
